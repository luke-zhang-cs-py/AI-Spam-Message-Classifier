scikit-learn>=1.3
pandas>=2.0
joblib>=1.3

# Only needed for the web UI (app.py); the CLI runs without it.
Flask>=2.3

# Tests only.
pytest>=7.4

# Also tests only, and less optional than they look. `coverage` is what
# tests/test_published_figures.py measures the published figures with, so
# without it that test errors rather than skips -- and CI installs exactly
# this file. pytest-cov is what the documented coverage command asks for.
coverage>=7.0
pytest-cov>=4.0
