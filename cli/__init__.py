"""The command line: train it, then ask it about a message.

    python -m cli.train_spam_classifier
    python -m cli.classify "your message here"

Both are thin. The decisions are in pipeline/.
"""
