[pytest]
# Collect from tests/ only.
#
# The project root holds four application modules and two CLI entry points. A
# stray file there matching test_*.py gets imported and run, and with the same
# basename as one in tests/ it collides outright -- which is exactly what
# happened while auditing this repository.
testpaths = tests
